'use client';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { CalendarDays, Images, Instagram, Plus, UserRound } from 'lucide-react';
export default function MobileNav(){ const path=usePathname(); const items=[['/dashboard','Home',UserRound],['/profiles','Profiles',Instagram],['/gallery','Gallery',Images],['/create','Create',Plus],['/schedule','Schedule',CalendarDays]] as const; return <nav className="fixed bottom-0 inset-x-0 z-40 lg:hidden bg-white/95 backdrop-blur border-t border-[#eceef4] px-2 py-2 grid grid-cols-5 gap-1 safe-bottom">{items.map(([href,label,Icon])=>{const active=path===href||path.startsWith(href+'/'); return <Link key={href} href={href} className={`flex flex-col items-center justify-center gap-1 rounded-xl py-2 text-[10px] font-semibold ${active?'text-[#f04444]':'text-[#73788b]'}`}><Icon size={18}/>{label}</Link>})}</nav> }