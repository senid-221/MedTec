import Link from 'next/link'; import { Plus } from 'lucide-react';
export default function SectionHeader({title,description,action,href}:{title:string;description?:string;action?:string;href?:string}){return <div className="flex flex-col sm:flex-row sm:items-end justify-between gap-4 mb-6"><div><h1 className="text-2xl sm:text-3xl font-bold tracking-tight">{title}</h1>{description&&<p className="text-sm text-[#7b8094] mt-1">{description}</p>}</div>{action&&href&&<Link href={href} className="inline-flex items-center justify-center gap-2 bg-[#f04444] text-white rounded-xl px-4 py-2.5 text-sm font-semibold"><Plus size={17}/>{action}</Link>}</div>}