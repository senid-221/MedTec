'use client'; import AppShell from '@/components/AppShell'; import SectionHeader from '@/components/SectionHeader'; import {KEYS,LocalPost,readLocal} from '@/lib/local'; import {useEffect,useState} from 'react';
export default function Bulk(){const [posts,setPosts]=useState<LocalPost[]>([]);useEffect(()=>setPosts(readLocal(KEYS.posts,[])),[]);return <AppShell><SectionHeader title="Bulk scheduling" description="Review and organize multiple scheduled items."/><div className="card p-5"><div className="grid grid-cols-2 sm:grid-cols-4 gap-3">{posts.slice(0,12).map((p,i)=><div key={p.id} className="rounded-xl bg-[#f7f8fb] p-4"><div className="text-xs text-[#7b8094]">Slot {i+1}</div><div className="font-semibold mt-1">{p.type}</div><div className="text-xs mt-2 truncate">{p.caption||'Empty caption'}</div></div>)}</div>{!posts.length&&<p className="text-center text-sm text-[#7b8094] py-10">Create posts first, then organize them here.</p>}</div></AppShell>}