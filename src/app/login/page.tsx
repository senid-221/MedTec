'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client'; import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();try{const s=createClient();const {error}=await s.auth.signInWithPassword({email,password});if(error)throw error;window.location.href='/dashboard'}catch(err){setMsg(err instanceof Error?err.message:'Login failed')}};return <main className="min-h-screen bg-[#202643] grid place-items-center p-5"><div className="w-full max-w-md rounded-3xl bg-white p-7 shadow-2xl"><img src="/logo.svg" className="h-10 mb-7" alt="MediTec"/><h1 className="text-2xl font-black">Welcome back</h1><p className="text-sm text-[#7b8094] mt-2">Private MediTec workspace.</p><form onSubmit={submit} className="space-y-3 mt-6"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full rounded-xl border border-[#eceef4] bg-[#f8f9fb] p-3"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full rounded-xl border border-[#eceef4] bg-[#f8f9fb] p-3"/><button className="w-full rounded-xl bg-[#f04444] text-white p-3 font-semibold">Login</button></form>{msg&&<p className="text-sm text-red-500 mt-3">{msg}</p>}<Link href="/" className="block text-center text-sm text-[#5869ef] mt-5">Back to Home</Link></div></main>}